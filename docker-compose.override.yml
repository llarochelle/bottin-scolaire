services:
  backend:
    ports:
      - "8001:8000"

    volumes:
      - ./backend:/app
      - ./storage:/storage

    command: >
      uvicorn server:app
      --host 0.0.0.0
      --port 8000
      --reload
